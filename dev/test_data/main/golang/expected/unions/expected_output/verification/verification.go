// Package verification allows you to verify model instances.
//
// The main function is [Verify].
//
// Other verification functions (`Verify*`) are left for modularity, in case you want
// to be explicit about the typing in your code. However, in the large majority of
// the cases, you only want to call [Verify].
package verification

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

import (
	"math/big"
	"fmt"
	"regexp"
	"strconv"
	"strings"
	aascommon "github.com/dummy-works/dummy/common"
	aasconstants "github.com/dummy-works/dummy/constants"
	aasreporting "github.com/dummy-works/dummy/reporting"
	aastypes "github.com/dummy-works/dummy/types"
)

// Represent a verification violation.
//
// Implements `error`.
type VerificationError struct{
	Path *aasreporting.Path
	Message string
}

func newVerificationError(message string) *VerificationError {
	return &VerificationError{
		Path: &aasreporting.Path{},
		Message: message,
	}
}

func (ve *VerificationError) Error() string {
	return fmt.Sprintf(
		"%s: %s",
		ve.PathString(),
		ve.Message,
	)
}

// Render the path as a string.
func (ve *VerificationError) PathString() string {
	return aasreporting.ToGolangPath(ve.Path)
}

// Verify `that` instance of [aastypes.IStructuralFirst].
//
// You have to supply the callback `onError` to iterate over the errors.
// If `onError` returns abort `true`, this function will abort
// further verification as well, and return abort `true`. Otherwise,
// abort `false` is returned.
func VerifyStructuralFirst(
	that aastypes.IStructuralFirst,
	onError func(*VerificationError) bool,
) (abort bool) {
	abort = false

	// No verification has been defined for IStructuralFirst.

	return
}

// Verify `that` instance of [aastypes.IStructuralSecond].
//
// You have to supply the callback `onError` to iterate over the errors.
// If `onError` returns abort `true`, this function will abort
// further verification as well, and return abort `true`. Otherwise,
// abort `false` is returned.
func VerifyStructuralSecond(
	that aastypes.IStructuralSecond,
	onError func(*VerificationError) bool,
) (abort bool) {
	abort = false

	// No verification has been defined for IStructuralSecond.

	return
}

// Verify `that` instance of [aastypes.IMixedAbstractDescendantOne].
//
// You have to supply the callback `onError` to iterate over the errors.
// If `onError` returns abort `true`, this function will abort
// further verification as well, and return abort `true`. Otherwise,
// abort `false` is returned.
func VerifyMixedAbstractDescendantOne(
	that aastypes.IMixedAbstractDescendantOne,
	onError func(*VerificationError) bool,
) (abort bool) {
	abort = false

	// No verification has been defined for IMixedAbstractDescendantOne.

	return
}

// Verify `that` instance of [aastypes.IMixedAbstractDescendantTwo].
//
// You have to supply the callback `onError` to iterate over the errors.
// If `onError` returns abort `true`, this function will abort
// further verification as well, and return abort `true`. Otherwise,
// abort `false` is returned.
func VerifyMixedAbstractDescendantTwo(
	that aastypes.IMixedAbstractDescendantTwo,
	onError func(*VerificationError) bool,
) (abort bool) {
	abort = false

	// No verification has been defined for IMixedAbstractDescendantTwo.

	return
}

// Verify `that` instance of [aastypes.IMixedConcreteWithDescendants].
//
// You have to supply the callback `onError` to iterate over the errors.
// If `onError` returns abort `true`, this function will abort
// further verification as well, and return abort `true`. Otherwise,
// abort `false` is returned.
func VerifyMixedConcreteWithDescendants(
	that aastypes.IMixedConcreteWithDescendants,
	onError func(*VerificationError) bool,
) (abort bool) {
	abort = false

	// No verification has been defined for IMixedConcreteWithDescendants.

	return
}

// Verify `that` instance of [aastypes.IMixedConcreteWithDescendantsChild].
//
// You have to supply the callback `onError` to iterate over the errors.
// If `onError` returns abort `true`, this function will abort
// further verification as well, and return abort `true`. Otherwise,
// abort `false` is returned.
func VerifyMixedConcreteWithDescendantsChild(
	that aastypes.IMixedConcreteWithDescendantsChild,
	onError func(*VerificationError) bool,
) (abort bool) {
	abort = false

	// No verification has been defined for IMixedConcreteWithDescendantsChild.

	return
}

// Verify `that` instance of [aastypes.IMixedConcreteLeaf].
//
// You have to supply the callback `onError` to iterate over the errors.
// If `onError` returns abort `true`, this function will abort
// further verification as well, and return abort `true`. Otherwise,
// abort `false` is returned.
func VerifyMixedConcreteLeaf(
	that aastypes.IMixedConcreteLeaf,
	onError func(*VerificationError) bool,
) (abort bool) {
	abort = false

	// No verification has been defined for IMixedConcreteLeaf.

	return
}

// Verify `that` instance of [aastypes.IModelTypedFirst].
//
// You have to supply the callback `onError` to iterate over the errors.
// If `onError` returns abort `true`, this function will abort
// further verification as well, and return abort `true`. Otherwise,
// abort `false` is returned.
func VerifyModelTypedFirst(
	that aastypes.IModelTypedFirst,
	onError func(*VerificationError) bool,
) (abort bool) {
	abort = false

	// No verification has been defined for IModelTypedFirst.

	return
}

// Verify `that` instance of [aastypes.IModelTypedSecond].
//
// You have to supply the callback `onError` to iterate over the errors.
// If `onError` returns abort `true`, this function will abort
// further verification as well, and return abort `true`. Otherwise,
// abort `false` is returned.
func VerifyModelTypedSecond(
	that aastypes.IModelTypedSecond,
	onError func(*VerificationError) bool,
) (abort bool) {
	abort = false

	// No verification has been defined for IModelTypedSecond.

	return
}

// Verify `that` instance of [aastypes.ISomething].
//
// You have to supply the callback `onError` to iterate over the errors.
// If `onError` returns abort `true`, this function will abort
// further verification as well, and return abort `true`. Otherwise,
// abort `false` is returned.
func VerifySomething(
	that aastypes.ISomething,
	onError func(*VerificationError) bool,
) (abort bool) {
	abort = false

	if that.StructuralProperty() == nil {
		abort = onError(
			newVerificationError(
				"Required property not set: StructuralProperty",
			),
		)
		if abort {
			return
		}
	} else {
		abort = Verify(
			that.StructuralProperty().Underlying(),
			func(err *VerificationError) bool {
				err.Path.PrependName(
					&aasreporting.NameSegment{
						Name: "StructuralProperty",
					},
				)
				return onError(err)
			},
		)
		if abort {
			return
		}
	}

	if that.MixedProperty() == nil {
		abort = onError(
			newVerificationError(
				"Required property not set: MixedProperty",
			),
		)
		if abort {
			return
		}
	} else {
		abort = Verify(
			that.MixedProperty().Underlying(),
			func(err *VerificationError) bool {
				err.Path.PrependName(
					&aasreporting.NameSegment{
						Name: "MixedProperty",
					},
				)
				return onError(err)
			},
		)
		if abort {
			return
		}
	}

	if that.ModelTypedProperty() == nil {
		abort = onError(
			newVerificationError(
				"Required property not set: ModelTypedProperty",
			),
		)
		if abort {
			return
		}
	} else {
		abort = Verify(
			that.ModelTypedProperty().Underlying(),
			func(err *VerificationError) bool {
				err.Path.PrependName(
					&aasreporting.NameSegment{
						Name: "ModelTypedProperty",
					},
				)
				return onError(err)
			},
		)
		if abort {
			return
		}
	}

	if that.ListStructuralProperty() == nil {
		abort = onError(
			newVerificationError(
				"Required property not set: ListStructuralProperty",
			),
		)
		if abort {
			return
		}
	} else {
		for i, v := range that.ListStructuralProperty() {
			abort = Verify(
				v.Underlying(),
				func(err *VerificationError) bool {
					err.Path.PrependIndex(
						&aasreporting.IndexSegment{
							Index: i,
						},
					)

					err.Path.PrependName(
						&aasreporting.NameSegment{
							Name: "ListStructuralProperty",
						},
					)

					return onError(err)
				},
			)
			if abort {
				return
			}
		}
	}

	if that.ListMixedProperty() == nil {
		abort = onError(
			newVerificationError(
				"Required property not set: ListMixedProperty",
			),
		)
		if abort {
			return
		}
	} else {
		for i, v := range that.ListMixedProperty() {
			abort = Verify(
				v.Underlying(),
				func(err *VerificationError) bool {
					err.Path.PrependIndex(
						&aasreporting.IndexSegment{
							Index: i,
						},
					)

					err.Path.PrependName(
						&aasreporting.NameSegment{
							Name: "ListMixedProperty",
						},
					)

					return onError(err)
				},
			)
			if abort {
				return
			}
		}
	}

	if that.ListModelTypedProperty() == nil {
		abort = onError(
			newVerificationError(
				"Required property not set: ListModelTypedProperty",
			),
		)
		if abort {
			return
		}
	} else {
		for i, v := range that.ListModelTypedProperty() {
			abort = Verify(
				v.Underlying(),
				func(err *VerificationError) bool {
					err.Path.PrependIndex(
						&aasreporting.IndexSegment{
							Index: i,
						},
					)

					err.Path.PrependName(
						&aasreporting.NameSegment{
							Name: "ListModelTypedProperty",
						},
					)

					return onError(err)
				},
			)
			if abort {
				return
			}
		}
	}

	abort = Verify(
		that.TupleProperty().Item1.Underlying(),
		func(err *VerificationError) bool {
			err.Path.PrependIndex(
				&aasreporting.IndexSegment{
					Index: 0,
				},
			)

			err.Path.PrependName(
				&aasreporting.NameSegment{
					Name: "TupleProperty",
				},
			)

			return onError(err)
		},
	)
	if abort {
		return
	}

	abort = Verify(
		that.TupleProperty().Item2.Underlying(),
		func(err *VerificationError) bool {
			err.Path.PrependIndex(
				&aasreporting.IndexSegment{
					Index: 1,
				},
			)

			err.Path.PrependName(
				&aasreporting.NameSegment{
					Name: "TupleProperty",
				},
			)

			return onError(err)
		},
	)
	if abort {
		return
	}

	abort = Verify(
		that.TupleProperty().Item3.Underlying(),
		func(err *VerificationError) bool {
			err.Path.PrependIndex(
				&aasreporting.IndexSegment{
					Index: 2,
				},
			)

			err.Path.PrependName(
				&aasreporting.NameSegment{
					Name: "TupleProperty",
				},
			)

			return onError(err)
		},
	)
	if abort {
		return
	}

	if that.OptionalStructuralProperty() != nil {
		abort = Verify(
			that.OptionalStructuralProperty().Underlying(),
			func(err *VerificationError) bool {
				err.Path.PrependName(
					&aasreporting.NameSegment{
						Name: "OptionalStructuralProperty",
					},
				)
				return onError(err)
			},
		)
		if abort {
			return
		}
	}

	if that.OptionalMixedProperty() != nil {
		abort = Verify(
			that.OptionalMixedProperty().Underlying(),
			func(err *VerificationError) bool {
				err.Path.PrependName(
					&aasreporting.NameSegment{
						Name: "OptionalMixedProperty",
					},
				)
				return onError(err)
			},
		)
		if abort {
			return
		}
	}

	if that.OptionalModelTypedProperty() != nil {
		abort = Verify(
			that.OptionalModelTypedProperty().Underlying(),
			func(err *VerificationError) bool {
				err.Path.PrependName(
					&aasreporting.NameSegment{
						Name: "OptionalModelTypedProperty",
					},
				)
				return onError(err)
			},
		)
		if abort {
			return
		}
	}

	return
}

// Verify ``that`` instance.
//
// You have to supply the callback `onError` to iterate over the errors.
// If `onError` returns abort `true`, this function will abort
// further verification as well, and return abort `true`. Otherwise,
// abort `false` is returned.
func Verify(
	that aastypes.IClass,
	onError func(*VerificationError) bool,
) (abort bool) {
	modelType := that.ModelType()
	switch modelType {
	case aastypes.ModelTypeStructuralFirst:
		abort = VerifyStructuralFirst(
			that.(aastypes.IStructuralFirst),
			onError,
		)
	case aastypes.ModelTypeStructuralSecond:
		abort = VerifyStructuralSecond(
			that.(aastypes.IStructuralSecond),
			onError,
		)
	case aastypes.ModelTypeMixedAbstractDescendantOne:
		abort = VerifyMixedAbstractDescendantOne(
			that.(aastypes.IMixedAbstractDescendantOne),
			onError,
		)
	case aastypes.ModelTypeMixedAbstractDescendantTwo:
		abort = VerifyMixedAbstractDescendantTwo(
			that.(aastypes.IMixedAbstractDescendantTwo),
			onError,
		)
	case aastypes.ModelTypeMixedConcreteWithDescendants:
		abort = VerifyMixedConcreteWithDescendants(
			that.(aastypes.IMixedConcreteWithDescendants),
			onError,
		)
	case aastypes.ModelTypeMixedConcreteWithDescendantsChild:
		abort = VerifyMixedConcreteWithDescendantsChild(
			that.(aastypes.IMixedConcreteWithDescendantsChild),
			onError,
		)
	case aastypes.ModelTypeMixedConcreteLeaf:
		abort = VerifyMixedConcreteLeaf(
			that.(aastypes.IMixedConcreteLeaf),
			onError,
		)
	case aastypes.ModelTypeModelTypedFirst:
		abort = VerifyModelTypedFirst(
			that.(aastypes.IModelTypedFirst),
			onError,
		)
	case aastypes.ModelTypeModelTypedSecond:
		abort = VerifyModelTypedSecond(
			that.(aastypes.IModelTypedSecond),
			onError,
		)
	case aastypes.ModelTypeSomething:
		abort = VerifySomething(
			that.(aastypes.ISomething),
			onError,
		)
	default:
		abort = onError(
			newVerificationError(
				fmt.Sprintf(
					"Unexpected model type literal: %v",
					modelType,
				),
			),
		)
	}
	return
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
