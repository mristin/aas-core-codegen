// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "./common.hpp"
#include "./common_examples.hpp"
#include "./common_xmlization.hpp"

#include <dummy/iteration.hpp>
#include <dummy/stringification.hpp>
#include <dummy/types.hpp>

#define CATCH_CONFIG_MAIN
#include <catch2/catch.hpp>

#include <deque>

namespace aas = dummy;

const std::filesystem::path& DetermineXmlDir() {
  static aas::common::optional<std::filesystem::path> result;
  if (!result.has_value()) {
    result = test::common::DetermineTestDataDir() / "Xml";
  }

  return *result;
}

const std::filesystem::path& DetermineDescentDir() {
  static aas::common::optional<std::filesystem::path> result;
  if (!result.has_value()) {
    result = test::common::DetermineTestDataDir() / "Descent";
  }

  return *result;
}

const std::filesystem::path& DetermineDescentOnceDir() {
  static aas::common::optional<std::filesystem::path> result;
  if (!result.has_value()) {
    result = test::common::DetermineTestDataDir() / "DescentOnce";
  }

  return *result;
}

void AssertOrRerecordDescent(
  const std::filesystem::path& xml_path,
  const std::filesystem::path& trace_path
) {
  std::shared_ptr<
    aas::types::IClass
  > instance(
    test::common::xmlization::MustDeserializeFile(xml_path)
  );

  std::deque<std::wstring> parts;

  for (
    const std::shared_ptr<aas::types::IClass>& something
    : aas::iteration::Descent(instance)
  ) {
    parts.emplace_back(test::common::TraceMark(*something));
    parts.push_back(L"\n");
  }

  test::common::AssertContentEqualsExpectedOrRecord(
    aas::common::WstringToUtf8(
      test::common::JoinWstrings(parts, L"")
    ),
    trace_path
  );
}

void AssertOrRerecordDescentOnce(
  const std::shared_ptr<aas::types::IClass>& instance,
  const std::filesystem::path& trace_path
) {
  std::deque<std::wstring> parts;

  for (
    const std::shared_ptr<aas::types::IClass>& something
    : aas::iteration::DescentOnce(instance)
  ) {
    parts.emplace_back(test::common::TraceMark(*something));
    parts.push_back(L"\n");
  }

  test::common::AssertContentEqualsExpectedOrRecord(
    aas::common::WstringToUtf8(
      test::common::JoinWstrings(parts, L"")
    ),
    trace_path
  );
}

TEST_CASE("Test Descent over an StructuralFirst") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "structuralFirst",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an StructuralFirst") {
  const std::shared_ptr<
    aas::types::IStructuralFirst
  > instance(
    test::common::examples::LoadMaxStructuralFirst()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxStructuralFirst.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an StructuralSecond") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "structuralSecond",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an StructuralSecond") {
  const std::shared_ptr<
    aas::types::IStructuralSecond
  > instance(
    test::common::examples::LoadMaxStructuralSecond()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxStructuralSecond.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an MixedAbstractDescendantOne") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "mixedAbstractDescendantOne",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an MixedAbstractDescendantOne") {
  const std::shared_ptr<
    aas::types::IMixedAbstractDescendantOne
  > instance(
    test::common::examples::LoadMaxMixedAbstractDescendantOne()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxMixedAbstractDescendantOne.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an MixedAbstractDescendantTwo") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "mixedAbstractDescendantTwo",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an MixedAbstractDescendantTwo") {
  const std::shared_ptr<
    aas::types::IMixedAbstractDescendantTwo
  > instance(
    test::common::examples::LoadMaxMixedAbstractDescendantTwo()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxMixedAbstractDescendantTwo.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an MixedConcreteWithDescendants") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "mixedConcreteWithDescendants",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an MixedConcreteWithDescendants") {
  const std::shared_ptr<
    aas::types::IMixedConcreteWithDescendants
  > instance(
    test::common::examples::LoadMaxMixedConcreteWithDescendants()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxMixedConcreteWithDescendants.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an MixedConcreteWithDescendantsChild") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "mixedConcreteWithDescendantsChild",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an MixedConcreteWithDescendantsChild") {
  const std::shared_ptr<
    aas::types::IMixedConcreteWithDescendantsChild
  > instance(
    test::common::examples::LoadMaxMixedConcreteWithDescendantsChild()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxMixedConcreteWithDescendantsChild.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an MixedConcreteLeaf") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "mixedConcreteLeaf",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an MixedConcreteLeaf") {
  const std::shared_ptr<
    aas::types::IMixedConcreteLeaf
  > instance(
    test::common::examples::LoadMaxMixedConcreteLeaf()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxMixedConcreteLeaf.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an ModelTypedFirst") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "modelTypedFirst",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an ModelTypedFirst") {
  const std::shared_ptr<
    aas::types::IModelTypedFirst
  > instance(
    test::common::examples::LoadMaxModelTypedFirst()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxModelTypedFirst.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an ModelTypedSecond") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "modelTypedSecond",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an ModelTypedSecond") {
  const std::shared_ptr<
    aas::types::IModelTypedSecond
  > instance(
    test::common::examples::LoadMaxModelTypedSecond()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxModelTypedSecond.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

TEST_CASE("Test Descent over an Something") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineXmlDir()
        / "Expected"
        / "something",
      ".xml"
    )
  );

  for (const std::filesystem::path& path : paths) {
    const std::filesystem::path parent(
      (
        DetermineDescentDir()
          / std::filesystem::relative(path, DetermineXmlDir())
      ).parent_path()
    );

    const std::filesystem::path trace_path(
      parent
        / (path.filename().string() + ".trace")
    );

    AssertOrRerecordDescent(path, trace_path);
  }
}

TEST_CASE("Test DescentOnce over an Something") {
  const std::shared_ptr<
    aas::types::ISomething
  > instance(
    test::common::examples::LoadMaxSomething()
  );

  const std::filesystem::path trace_path(
    DetermineDescentOnceDir()
      / "MaxSomething.trace"
  );

  AssertOrRerecordDescentOnce(instance, trace_path);
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
