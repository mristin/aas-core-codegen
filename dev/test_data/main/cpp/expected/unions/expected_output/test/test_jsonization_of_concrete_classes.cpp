// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.

#include "./common.hpp"
#include "./common_jsonization.hpp"

#include <dummy/jsonization.hpp>

#define CATCH_CONFIG_MAIN
#include <catch2/catch.hpp>

namespace aas = dummy;

template<class ClassT>
void AssertRoundTrip(
  const std::filesystem::path& path,
  std::function<
    aas::common::expected<
      std::shared_ptr<ClassT>,
      aas::jsonization::DeserializationError
    >(const nlohmann::json&, bool)
  > deserialization_function
) {
  const nlohmann::json json = test::common::jsonization::MustReadJson(path);

  aas::common::expected<
    std::shared_ptr<ClassT>,
    aas::jsonization::DeserializationError
  > deserialized = deserialization_function(json, false);

  if (!deserialized.has_value()) {
    INFO(
      aas::common::Concat(
        "Failed to de-serialize from ",
        path.string(),
        ": ",
        aas::common::WstringToUtf8(
          deserialized.error().path.ToWstring()
        ),
        ": ",
        aas::common::WstringToUtf8(
          deserialized.error().cause
        )
      )
    )
    REQUIRE(deserialized.has_value());
  }

  nlohmann::json another_json = aas::jsonization::Serialize(
    *(deserialized.value())
  );

  std::optional<std::string> diff_message = test::common::jsonization::CompareJsons(
    json,
    another_json
  );
  if (diff_message.has_value()) {
    INFO(
      aas::common::Concat(
        "The JSON round-trip from ",
        path.string(),
        " failed. There is a diff between the original JSON "
        "and the serialized one: ",
        *diff_message
      )
    )
    REQUIRE(!diff_message.has_value());
  }
}

template<typename ClassT>
void AssertDeserializationFailure(
  const std::filesystem::path& path,
  std::function<
    aas::common::expected<
      std::shared_ptr<ClassT>,
      aas::jsonization::DeserializationError
    >(const nlohmann::json&, bool)
  > deserialization_function,
  const std::filesystem::path& error_path
) {
  const nlohmann::json json = test::common::jsonization::MustReadJson(path);

  aas::common::expected<
    std::shared_ptr<ClassT>,
    aas::jsonization::DeserializationError
  > deserialized = deserialization_function(json, false);

  if (deserialized.has_value()) {
    INFO(
      aas::common::Concat(
        "Expected the de-serialization to fail on ",
        path.string(),
        ", but the de-serialization succeeded"
      )
    )
    REQUIRE(!deserialized.has_value());
  }

  test::common::AssertContentEqualsExpectedOrRecord(
    aas::common::Concat(
      aas::common::WstringToUtf8(
        deserialized.error().path.ToWstring()
      ),
      ": ",
      aas::common::WstringToUtf8(
        deserialized.error().cause
      )
    ),
    error_path
  );
}

const std::filesystem::path& DetermineJsonDir() {
  static aas::common::optional<std::filesystem::path> result;
  if (!result.has_value()) {
    result = test::common::DetermineTestDataDir() / "Json";
  }

  return *result;
}

const std::filesystem::path& DetermineErrorDir() {
  static aas::common::optional<std::filesystem::path> result;
  if (!result.has_value()) {
    result = test::common::DetermineTestDataDir() / "JsonizationError";
  }

  return *result;
}

TEST_CASE("Test the round-trip of an expected StructuralFirst") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineJsonDir()
        / "Expected"
        / "StructuralFirst",
      ".json"
    )
  );

  for (const std::filesystem::path& path : paths) {
    AssertRoundTrip<
      aas::types::IStructuralFirst
    >(path, aas::jsonization::StructuralFirstFrom);
  }
}

TEST_CASE("Test the de-serialization failure on an unexpected StructuralFirst") {
  for (
    const std::filesystem::path& causeDir
    : test::common::ListSubdirectories(
      DetermineJsonDir()
        / "Unexpected"
        / "Unserializable"
    )
  ) {
    for (
      const std::filesystem::path& path
      : test::common::FindFilesBySuffixRecursively(
        causeDir / "StructuralFirst",
        ".json"
      )
    ) {
      const std::filesystem::path parent(
        (
          DetermineErrorDir()
          / std::filesystem::relative(path, DetermineJsonDir())
        ).parent_path()
      );

      const std::filesystem::path error_path(
        parent
        / (path.filename().string() + ".error")
      );

      AssertDeserializationFailure<
        aas::types::IStructuralFirst
      >(
        path,
        aas::jsonization::StructuralFirstFrom,
        error_path
      );
    }
  }
}

TEST_CASE("Test the round-trip of an expected StructuralSecond") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineJsonDir()
        / "Expected"
        / "StructuralSecond",
      ".json"
    )
  );

  for (const std::filesystem::path& path : paths) {
    AssertRoundTrip<
      aas::types::IStructuralSecond
    >(path, aas::jsonization::StructuralSecondFrom);
  }
}

TEST_CASE("Test the de-serialization failure on an unexpected StructuralSecond") {
  for (
    const std::filesystem::path& causeDir
    : test::common::ListSubdirectories(
      DetermineJsonDir()
        / "Unexpected"
        / "Unserializable"
    )
  ) {
    for (
      const std::filesystem::path& path
      : test::common::FindFilesBySuffixRecursively(
        causeDir / "StructuralSecond",
        ".json"
      )
    ) {
      const std::filesystem::path parent(
        (
          DetermineErrorDir()
          / std::filesystem::relative(path, DetermineJsonDir())
        ).parent_path()
      );

      const std::filesystem::path error_path(
        parent
        / (path.filename().string() + ".error")
      );

      AssertDeserializationFailure<
        aas::types::IStructuralSecond
      >(
        path,
        aas::jsonization::StructuralSecondFrom,
        error_path
      );
    }
  }
}

TEST_CASE("Test the round-trip of an expected MixedAbstractDescendantOne") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineJsonDir()
        / "Expected"
        / "MixedAbstractDescendantOne",
      ".json"
    )
  );

  for (const std::filesystem::path& path : paths) {
    AssertRoundTrip<
      aas::types::IMixedAbstractDescendantOne
    >(path, aas::jsonization::MixedAbstractDescendantOneFrom);
  }
}

TEST_CASE("Test the de-serialization failure on an unexpected MixedAbstractDescendantOne") {
  for (
    const std::filesystem::path& causeDir
    : test::common::ListSubdirectories(
      DetermineJsonDir()
        / "Unexpected"
        / "Unserializable"
    )
  ) {
    for (
      const std::filesystem::path& path
      : test::common::FindFilesBySuffixRecursively(
        causeDir / "MixedAbstractDescendantOne",
        ".json"
      )
    ) {
      const std::filesystem::path parent(
        (
          DetermineErrorDir()
          / std::filesystem::relative(path, DetermineJsonDir())
        ).parent_path()
      );

      const std::filesystem::path error_path(
        parent
        / (path.filename().string() + ".error")
      );

      AssertDeserializationFailure<
        aas::types::IMixedAbstractDescendantOne
      >(
        path,
        aas::jsonization::MixedAbstractDescendantOneFrom,
        error_path
      );
    }
  }
}

TEST_CASE("Test the round-trip of an expected MixedAbstractDescendantTwo") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineJsonDir()
        / "Expected"
        / "MixedAbstractDescendantTwo",
      ".json"
    )
  );

  for (const std::filesystem::path& path : paths) {
    AssertRoundTrip<
      aas::types::IMixedAbstractDescendantTwo
    >(path, aas::jsonization::MixedAbstractDescendantTwoFrom);
  }
}

TEST_CASE("Test the de-serialization failure on an unexpected MixedAbstractDescendantTwo") {
  for (
    const std::filesystem::path& causeDir
    : test::common::ListSubdirectories(
      DetermineJsonDir()
        / "Unexpected"
        / "Unserializable"
    )
  ) {
    for (
      const std::filesystem::path& path
      : test::common::FindFilesBySuffixRecursively(
        causeDir / "MixedAbstractDescendantTwo",
        ".json"
      )
    ) {
      const std::filesystem::path parent(
        (
          DetermineErrorDir()
          / std::filesystem::relative(path, DetermineJsonDir())
        ).parent_path()
      );

      const std::filesystem::path error_path(
        parent
        / (path.filename().string() + ".error")
      );

      AssertDeserializationFailure<
        aas::types::IMixedAbstractDescendantTwo
      >(
        path,
        aas::jsonization::MixedAbstractDescendantTwoFrom,
        error_path
      );
    }
  }
}

TEST_CASE("Test the round-trip of an expected MixedConcreteWithDescendants") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineJsonDir()
        / "Expected"
        / "MixedConcreteWithDescendants",
      ".json"
    )
  );

  for (const std::filesystem::path& path : paths) {
    AssertRoundTrip<
      aas::types::IMixedConcreteWithDescendants
    >(path, aas::jsonization::MixedConcreteWithDescendantsFrom);
  }
}

TEST_CASE("Test the de-serialization failure on an unexpected MixedConcreteWithDescendants") {
  for (
    const std::filesystem::path& causeDir
    : test::common::ListSubdirectories(
      DetermineJsonDir()
        / "Unexpected"
        / "Unserializable"
    )
  ) {
    for (
      const std::filesystem::path& path
      : test::common::FindFilesBySuffixRecursively(
        causeDir / "MixedConcreteWithDescendants",
        ".json"
      )
    ) {
      const std::filesystem::path parent(
        (
          DetermineErrorDir()
          / std::filesystem::relative(path, DetermineJsonDir())
        ).parent_path()
      );

      const std::filesystem::path error_path(
        parent
        / (path.filename().string() + ".error")
      );

      AssertDeserializationFailure<
        aas::types::IMixedConcreteWithDescendants
      >(
        path,
        aas::jsonization::MixedConcreteWithDescendantsFrom,
        error_path
      );
    }
  }
}

TEST_CASE("Test the round-trip of an expected MixedConcreteWithDescendantsChild") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineJsonDir()
        / "Expected"
        / "MixedConcreteWithDescendantsChild",
      ".json"
    )
  );

  for (const std::filesystem::path& path : paths) {
    AssertRoundTrip<
      aas::types::IMixedConcreteWithDescendantsChild
    >(path, aas::jsonization::MixedConcreteWithDescendantsChildFrom);
  }
}

TEST_CASE("Test the de-serialization failure on an unexpected MixedConcreteWithDescendantsChild") {
  for (
    const std::filesystem::path& causeDir
    : test::common::ListSubdirectories(
      DetermineJsonDir()
        / "Unexpected"
        / "Unserializable"
    )
  ) {
    for (
      const std::filesystem::path& path
      : test::common::FindFilesBySuffixRecursively(
        causeDir / "MixedConcreteWithDescendantsChild",
        ".json"
      )
    ) {
      const std::filesystem::path parent(
        (
          DetermineErrorDir()
          / std::filesystem::relative(path, DetermineJsonDir())
        ).parent_path()
      );

      const std::filesystem::path error_path(
        parent
        / (path.filename().string() + ".error")
      );

      AssertDeserializationFailure<
        aas::types::IMixedConcreteWithDescendantsChild
      >(
        path,
        aas::jsonization::MixedConcreteWithDescendantsChildFrom,
        error_path
      );
    }
  }
}

TEST_CASE("Test the round-trip of an expected MixedConcreteLeaf") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineJsonDir()
        / "Expected"
        / "MixedConcreteLeaf",
      ".json"
    )
  );

  for (const std::filesystem::path& path : paths) {
    AssertRoundTrip<
      aas::types::IMixedConcreteLeaf
    >(path, aas::jsonization::MixedConcreteLeafFrom);
  }
}

TEST_CASE("Test the de-serialization failure on an unexpected MixedConcreteLeaf") {
  for (
    const std::filesystem::path& causeDir
    : test::common::ListSubdirectories(
      DetermineJsonDir()
        / "Unexpected"
        / "Unserializable"
    )
  ) {
    for (
      const std::filesystem::path& path
      : test::common::FindFilesBySuffixRecursively(
        causeDir / "MixedConcreteLeaf",
        ".json"
      )
    ) {
      const std::filesystem::path parent(
        (
          DetermineErrorDir()
          / std::filesystem::relative(path, DetermineJsonDir())
        ).parent_path()
      );

      const std::filesystem::path error_path(
        parent
        / (path.filename().string() + ".error")
      );

      AssertDeserializationFailure<
        aas::types::IMixedConcreteLeaf
      >(
        path,
        aas::jsonization::MixedConcreteLeafFrom,
        error_path
      );
    }
  }
}

TEST_CASE("Test the round-trip of an expected ModelTypedFirst") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineJsonDir()
        / "Expected"
        / "ModelTypedFirst",
      ".json"
    )
  );

  for (const std::filesystem::path& path : paths) {
    AssertRoundTrip<
      aas::types::IModelTypedFirst
    >(path, aas::jsonization::ModelTypedFirstFrom);
  }
}

TEST_CASE("Test the de-serialization failure on an unexpected ModelTypedFirst") {
  for (
    const std::filesystem::path& causeDir
    : test::common::ListSubdirectories(
      DetermineJsonDir()
        / "Unexpected"
        / "Unserializable"
    )
  ) {
    for (
      const std::filesystem::path& path
      : test::common::FindFilesBySuffixRecursively(
        causeDir / "ModelTypedFirst",
        ".json"
      )
    ) {
      const std::filesystem::path parent(
        (
          DetermineErrorDir()
          / std::filesystem::relative(path, DetermineJsonDir())
        ).parent_path()
      );

      const std::filesystem::path error_path(
        parent
        / (path.filename().string() + ".error")
      );

      AssertDeserializationFailure<
        aas::types::IModelTypedFirst
      >(
        path,
        aas::jsonization::ModelTypedFirstFrom,
        error_path
      );
    }
  }
}

TEST_CASE("Test the round-trip of an expected ModelTypedSecond") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineJsonDir()
        / "Expected"
        / "ModelTypedSecond",
      ".json"
    )
  );

  for (const std::filesystem::path& path : paths) {
    AssertRoundTrip<
      aas::types::IModelTypedSecond
    >(path, aas::jsonization::ModelTypedSecondFrom);
  }
}

TEST_CASE("Test the de-serialization failure on an unexpected ModelTypedSecond") {
  for (
    const std::filesystem::path& causeDir
    : test::common::ListSubdirectories(
      DetermineJsonDir()
        / "Unexpected"
        / "Unserializable"
    )
  ) {
    for (
      const std::filesystem::path& path
      : test::common::FindFilesBySuffixRecursively(
        causeDir / "ModelTypedSecond",
        ".json"
      )
    ) {
      const std::filesystem::path parent(
        (
          DetermineErrorDir()
          / std::filesystem::relative(path, DetermineJsonDir())
        ).parent_path()
      );

      const std::filesystem::path error_path(
        parent
        / (path.filename().string() + ".error")
      );

      AssertDeserializationFailure<
        aas::types::IModelTypedSecond
      >(
        path,
        aas::jsonization::ModelTypedSecondFrom,
        error_path
      );
    }
  }
}

TEST_CASE("Test the round-trip of an expected Something") {
  const std::deque<std::filesystem::path> paths(
    test::common::FindFilesBySuffixRecursively(
      DetermineJsonDir()
        / "Expected"
        / "Something",
      ".json"
    )
  );

  for (const std::filesystem::path& path : paths) {
    AssertRoundTrip<
      aas::types::ISomething
    >(path, aas::jsonization::SomethingFrom);
  }
}

TEST_CASE("Test the de-serialization failure on an unexpected Something") {
  for (
    const std::filesystem::path& causeDir
    : test::common::ListSubdirectories(
      DetermineJsonDir()
        / "Unexpected"
        / "Unserializable"
    )
  ) {
    for (
      const std::filesystem::path& path
      : test::common::FindFilesBySuffixRecursively(
        causeDir / "Something",
        ".json"
      )
    ) {
      const std::filesystem::path parent(
        (
          DetermineErrorDir()
          / std::filesystem::relative(path, DetermineJsonDir())
        ).parent_path()
      );

      const std::filesystem::path error_path(
        parent
        / (path.filename().string() + ".error")
      );

      AssertDeserializationFailure<
        aas::types::ISomething
      >(
        path,
        aas::jsonization::SomethingFrom,
        error_path
      );
    }
  }
}

// This code has been automatically generated by aas-core-codegen.
// Do NOT edit or append.
