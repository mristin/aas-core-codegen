The named union 'Some_union' can not be dispatched unambiguously during the de-serialization from JSON: the following implementer(s) do not have ``with_model_type`` set, so we would need to fall back to distinguishing them by their required properties, but they have no required properties of their own at all, so they can not be told apart from any other implementer this way: 'Another_class'. Either set ``with_model_type=True`` on the implementer(s) listed above, or make sure they each have at least one required property not shared with any other implementer of the union that also lacks ``with_model_type``.